const imagebuffer = getImageBuffer(imagePath);
const imageBuffer= getImageBuffer(imagePath);
const imagePath = "C:\Users\ATHARV VATS\tv-nft\images\th.jpeg";
}
return null;
  console.error("Error reading image file:", error);
} catch (error) {
 return imageBuffer;
 const imageBuffer = fs.readFileSync(filePath);
try {
function getImageBuffer(filepath) {
const fs = require("fs");
}
  console.error("Failed to get the image buffer.");
} else {
  console.log("Image buffer:", imageBuffer);
if (imageBuffer) {
const imageBuffer = getImageBuffer(imagePath);
const imagePath = "path/to/your/image.jpg"; // Replace with the path to your image file
// Usage example:
}
  }
    return null;
  } catch (error)     console.error("Error reading image file:", error);
    return imageBuffer;
    const imageBuffer = fs.readFileSync(filePath);
    // Read the image file synchronously
  try {
function getImageBuffer(filePath) {
// Read an image file and convert it into a buffer
const fs = require("fs");
node --version
truffle
truffle version
npm install -g truffle
console.log("hi")